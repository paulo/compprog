#include <iostream>

using namespace std;

// Since teams from the same office cannot work on the same project, it appears that we're
// working with a bipartite graph and the problem resembles the MVC
int main() {
    int n;
    cin >> n;

    

    return 0;
}
