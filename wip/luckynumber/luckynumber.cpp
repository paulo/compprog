#include <iostream>
#include <string>
#include <math.h>
#include <stdlib.h>

using namespace std;

typedef long long ll;

int main() {
    int n;
    cin >> n;

    // r[0] = '1';

    generate(0, n);
    // for(int i = 1; i < 10; i++) {
        // r = string(1000, '0');
        // r[0] = i+'0';
    // }

    cout << total << endl;
}
