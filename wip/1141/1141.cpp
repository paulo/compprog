#include <bits/stdc++.h>

using namespace std;
#define pb push_back

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n, best = INT_MIN, current = 0;
    cin >> n;

    unordered_map<int, int> m;  // tracks last seen index of element
    for (int i = 0; i < n; i++) {
    }

    return 0;
}
