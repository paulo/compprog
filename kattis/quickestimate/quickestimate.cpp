#include <bits/stdc++.h>

using namespace std;

#define pb push_back

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int n;
    cin >> n;

    string s;
    while (n-- > 0) {
        cin >> s;
        cout << s.length() << endl;
    }

    return 0;
}
